{
  "name": "proyecto-pruebas",
  "version": "1.0.0",
  "scripts": {
    "lint": "echo 'El código está bien formateado' && exit 0",
    "test": "echo '❌ ERROR CRÍTICO: Las etiquetas HTML no se cierran correctamente' && exit 1"
  }
}